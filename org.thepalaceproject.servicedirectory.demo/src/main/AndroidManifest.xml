<?xml version="1.0" encoding="UTF-8" ?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="org.thepalaceproject.servicedirectory.demo.MainApplication"
        android:allowBackup="true"
        android:contentDescription="ServiceDemo"
        android:theme="@style/Theme.AppCompat.Light"
        android:label="ServiceDemo">

        <!-- Main entry point for the application. -->
        <activity
            android:name="org.thepalaceproject.servicedirectory.demo.MainActivity"
            android:contentDescription="ServiceDemo"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
